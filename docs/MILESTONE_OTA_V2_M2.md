# OTA v2 M2 实施方案：断电日志、试运行与受限回滚

方案建立日期：2026-09-13。M2 建立在已经完成真机 A/v2 → B/v3 → A/v4 往返安装的 M1 上。M2-A 至 M2-C 的源码、主机故障模型和编译已完成；M2-D 已完成看门狗故障、安装断点复位、回滚断点复位，以及安装意图和标记切换完成后的两项物理断电真机验证。当前设备为 B/v16，A/v15 已退役；恢复日志 56 条有效、0 条损坏。后续每一次计划中的物理断电前仍要单独等待人工确认。

## 1. 目标和不可越过的边界

M2 要保证安装过程在可恢复的步骤间推进，并让设备上电后能够根据持久化记录判断应该继续安装、开始试运行、确认新版本或恢复旧版本。

这个方案不能保证任意损坏的新固件都自动回滚。TLSR8359 ROM 只按启动标记选择镜像，没有一个独立且不可变的第三方恢复程序。若候选固件损坏到无法执行最早期恢复代码，它就没有机会恢复旧槽，此时仍必须使用 SWire。只有未来引入受保护的小型 bootloader，才可能取消这项限制。

## 2. 已确认的 ROM 启动行为

Telink 多地址启动支持 `0x00000`、`0x20000` 和 `0x40000`，选择优先级固定为 `0x00000` 高于 `0x20000`，再高于 `0x40000`。因此：

- A、B 都有效时，ROM 总是先启动 A；元数据日志不能改变 ROM 的选择。
- 从 A 安装到 B 时，写好 B 标记后仍要清除 A 标记，才能进入 B 试运行。
- 从 B 安装到 A 时，A 标记一旦有效，下一次启动会直接进入 A，即使 B 标记尚未清除。
- `0x40000` 若作为备份扇区使用，其相对偏移 `0x08` 必须保持 `0xFF`，避免被 ROM 误识别成第三启动镜像。

## 3. Flash 规划

| 区域 | 地址 | M2 用途 |
|---|---:|---|
| 启动槽 A | `0x00000–0x1efff` | 应用槽 A |
| 预留 | `0x1f000–0x1ffff` | 暂不使用 |
| 启动槽 B | `0x20000–0x3efff` | 应用槽 B |
| 恢复日志 | `0x3f000–0x3ffff` | 4 KiB 追加式日志，128 条记录 |
| 旧槽首扇区备份 | `0x40000–0x40fff` | 安装前保存旧槽首 4 KiB，偏移 8 屏蔽为 `0xFF` |
| 系统/应用数据 | 其余高地址 | 不改变；尤其不碰 BLE MAC 和 `0x78100` 设置扇区 |

备份旧槽首扇区是必须的：旧标记从 `K` 清为 `0` 后，Flash 不能靠写入把 `0` 变回 `K`，只能擦除整个 4 KiB 扇区再恢复。因此回滚前必须有一份完整、读回一致且 CRC32 正确的首扇区副本。

## 4. 32 字节恢复记录

恢复日志只追加，不原地改写。每条记录为 32 字节：

| 偏移 | 长度 | 内容 |
|---:|---:|---|
| 0 | 4 | ASCII `H2RJ` |
| 4 | 1 | 格式版本，当前为 1 |
| 5 | 1 | 状态 |
| 6 | 1 | 来源槽 |
| 7 | 1 | 目标槽 |
| 8 | 4 | 单调序号 |
| 12 | 4 | 来源固件版本 |
| 16 | 4 | 目标固件版本 |
| 20 | 4 | 候选镜像 CRC32 |
| 24 | 4 | 屏蔽标记后的备份扇区 CRC32 |
| 28 | 4 | 前 28 字节 CRC32 |

状态依次为：`BASELINE`、`INSTALL_INTENT`、`MARKERS_SWITCHED`、`TRIAL_ATTEMPT`、`CONFIRMED`、`ROLLBACK_INTENT`、`ROLLBACK_DONE`。

扫描时忽略 CRC 错误的半写记录，以有效记录中序号最新者为准；发生半写后跳过已经变脏的 32 字节位置，继续写下一个全 `0xFF` 位置。只有处于已确认、至少一个启动槽确定有效且没有安装/回滚事务时，才允许擦除日志扇区做整理。

## 5. 安装与断电窗口

安装顺序冻结如下：

1. 完成 M1 的候选镜像全量校验，但候选启动标记仍为 `0xFF`。
2. 擦除 `0x40000`，复制当前槽首 4 KiB，同时把备份相对偏移 8 写成 `0xFF`；完整读回并核对 CRC32。
3. 追加 `INSTALL_INTENT`，记录备份 CRC、来源/目标版本和候选 CRC。
4. 写入并读回候选槽 `KNLT`。
5. 清除并读回来源槽首字节。
6. 追加 `MARKERS_SWITCHED` 并重启。

断电后的保守行为：

| 断电位置 | 可启动状态 | 恢复动作 |
|---|---|---|
| 备份或 `INSTALL_INTENT` 完成前 | 来源槽有效 | 放弃或重新开始安装 |
| `INSTALL_INTENT` 后、候选 `K` 前 | 仅来源槽有效 | 从校验点继续安装 |
| 候选 `K` 后、来源标记清除前 | 两槽有效 | 按 ROM 实际启动槽继续切换或进入试运行 |
| 来源标记清除后 | 仅候选槽有效 | 候选进入试运行；缺失的 `MARKERS_SWITCHED` 可补记 |
| 任一日志记录写到一半 | 启动标记取决于上一步 | 忽略半写记录，由上一条有效记录和两个真实标记共同判定 |

## 6. 试运行与回滚

候选固件必须把恢复判定放到正常 BLE/屏幕初始化之前。每次试运行尽早追加 `TRIAL_ATTEMPT` 并启动看门狗；完成最小自检、BLE 初始化并稳定进入广播后才追加 `CONFIRMED`，随后停止试运行看门狗。

连续达到上限仍未确认时，如果备份 CRC 有效，执行：

1. 追加 `ROLLBACK_INTENT`。
2. 擦除旧槽首扇区。
3. 从 `0x40000` 恢复完整首扇区，但相对偏移 8 暂时保持 `0xFF`；全扇区读回校验。
4. 最后单独写入旧槽 `KNLT` 并读回。
5. 清除失败候选的启动标记并读回。
6. 追加 `ROLLBACK_DONE` 并复位。

恢复过程中再次断电时，候选槽仍是唯一可启动代码；它必须根据 `ROLLBACK_INTENT` 重做擦除、复制和验证。若候选连这段早期代码都无法执行，则按第 1 节的边界使用 SWire。

## 7. 分阶段实施与验收

### M2-A：格式和主机故障模型

- [x] 固化 32 字节记录编码、CRC、扫描和序号回绕规则。
- [x] 对记录的每个截断长度和每个单字节损坏进行主机测试。
- [x] 固化 `0x40000` 备份标记屏蔽规则和 ROM 启动优先级模型。
- [x] 建立 `正常/继续安装/继续切槽/试运行/回滚/SWire` 的保守决策表。
- [x] 完成 TC32 固件编译，确认新模块不会突破镜像和 RAM 约束。

### M2-B：只记录，不自动回滚

- [x] 在安装前生成、读回并校验旧槽首扇区备份。
- [x] 接入追加式日志；任何备份或日志错误都禁止切换标记。
- [x] 完成只报告决策的中间实现，随后由 M2-C 接入受限回滚。
- [x] 主机模拟 A→B、B→A 每一个 Flash 写入及逐字节截断点。

### M2-C：试运行确认和受限回滚

- [x] 候选在 BLE 初始化前追加试运行次数，并配置 12 秒试运行看门狗。
- [x] BLE 初始化、广播配置和主循环稳定 5 秒后显式确认健康。
- [x] 连续 3 次启动未确认时按第 6 节恢复旧槽首扇区。
- [x] 主机模拟 A→B、B→A 回滚中每一个擦除、写入及逐字节截断点。

### M2-D：真机故障注入

- [x] 先安装显式标记的故障镜像：v7 暴露出普通定时看门狗会随 BLE 休眠暂停；修复为试运行窗口保持唤醒后，v8 已自动经历 3 次失败并恢复 v6，SWire 回读通过。
- [x] 软件注入在 `INSTALL_INTENT` 后复位（v10）和 `ROLLBACK_INTENT` 后复位（v12）均通过；设备能从日志重建候选参数并继续安装/回滚，SWire 回读通过。
- [x] 软件断点结果与主机模型一致后才进入物理断电；首项 A/v13 → B/v14 已在 `INSTALL_INTENT` 后、候选标记写入前完成真实断电恢复。
- [x] 首次物理断电前已单独停下，说明当前固件、切断时机和预期恢复结果，并取得人工确认；后续每次仍执行同样的确认门槛。
- [ ] 完成 A→B 与 B→A 两个方向的断电矩阵、显示、BLE 重连和待机功耗回归。

旧槽完整内容在 M2 内不擦除；只有后续版本已经重复确认健康、备份和回滚策略另有放行记录时，才讨论复用旧槽空间。

## 9. 2026-09-13 真机 M2-D 完成记录

- v5 通过 SWire 写入 A 槽建立 M2 基线；随后 BLE 安装 v6 到 B 槽并健康确认。安装日志 4 条全部有效，A/v5 首扇区备份读回一致。
- 显式故障 v7 首次验证发现 TLSR8258 普通定时看门狗在 BLE suspend 时不计时；v8 加入试运行期间 `SUSPEND_DISABLE` 后，故障镜像自动复位 3 次并完成回滚。回读结果：B/v6 逐字节一致，A/v8 退役，日志 18 条有效、0 条损坏。
- v9 验证健康试运行保持唤醒后正常确认；v10 在 `INSTALL_INTENT` 记录后软件复位，启动后自动重建会话并继续切槽，最终 B/v10 确认。
- v11 的回滚断点注入暴露了测试注入器在既有 `ROLLBACK_INTENT` 上重复复位的问题；用 SWire 将健康 v10 写回 A 槽救援后，修正为仅首次追加时注入。v12 在修正后于 `ROLLBACK_INTENT` 后复位，自动恢复 B/v10 成功。
- 最终 SWire 只读：A 标记为 `00NLT`、B 标记为 `KNLT`；B/v10 与产物逐字节一致；`0x40000` 备份 CRC32 为 `06F9956E` 且与屏蔽标记后的 B 首扇区一致；恢复日志共 40 条，CRC 全部正确、损坏数为 0。三色测试图命令已被当前固件接受。
- 为真实断电矩阵增加独立的 `HINK_OTA_PHYSICAL_CUT_POINT` 诊断开关；诊断固件会在持久化检查点永久等待，避免人工断电与正常执行竞争。该开关与软件复位注入互斥，普通构建默认关闭。
- A/v13 → B/v14 的首项物理断电已通过：v13 在记录序号 45 的 `INSTALL_INTENT` 完整落盘后、B 槽启动标记写入前等待；人工切断目标板 3.3 V 至少 3 秒并恢复后，A/v13 自动重建会话、完成标记切换，B/v14 记录 `TRIAL_ATTEMPT` 并在稳定 5 秒后记录 `CONFIRMED`。
- 断电恢复后 BLE 报告 B/v14、`CONFIRMED`、正常恢复动作、48 条有效日志和 0 条损坏。SWire 只读文件 `post_physical_install_intent_v14_0x00000_0x40fff.bin` 的 SHA-256 为 `e0897a2a510ed0940d85e4385027d48683868e673e36f264efe1c806e452feef`：A 标记 `00NLT` 且与退役 v13 匹配，B 标记 `KNLT` 且与 v14 逐字节一致；`0x40000` 备份与屏蔽标记后的 v13 首扇区一致，CRC32 为 `B600CB49`。三色测试图命令已被 v14 接受，画面目视结果待用户确认。
- A/v15 → B/v16 的第二项物理断电也已通过：v15 完成 B 槽 `KNLT`、A 槽 `00NLT` 和序号 54 的 `MARKERS_SWITCHED` 记录后，在复位前等待；恢复供电后 ROM 直接启动 B/v16，并依次追加序号 55 的 `TRIAL_ATTEMPT` 和序号 56 的 `CONFIRMED`。
- 第二次断电后的 BLE 状态为 B/v16、正常恢复动作、56 条有效日志和 0 条损坏。SWire 只读文件 `post_physical_markers_switched_v16_0x00000_0x40fff.bin` 的 SHA-256 为 `5ebb9c353ecb123086a8a726eb3f975debb8b58b22adf8de64b9e7d6739fd90b`：A 与退役 v15 匹配，B 与活动 v16 逐字节一致；备份与屏蔽标记后的 v15 首扇区一致，CRC32 为 `D2E76FE3`。三色测试图命令已重新发送并被 v16 接受，画面目视结果待用户确认。
- M2-D 剩余项目是继续覆盖安装和回滚的其他物理断电窗口、反向槽位、显示目视确认和待机功耗回归。每次人为断电前必须再次暂停并等待人工确认。

## 8. 2026-09-13 主机完成记录

- `firmware/src/ota_recovery.c` 实现记录编码、CRC、追加扫描、启动优先级和保守恢复决策；`ota.c` 接入 `0x3f000` 日志与 `0x40000` 屏蔽标记备份。
- 安装会在任何启动标记变化前完成旧槽首扇区备份、全量读回 CRC 和 `INSTALL_INTENT` 读回；随后记录 `MARKERS_SWITCHED`。
- 候选启动后在 BLE 初始化前追加 `TRIAL_ATTEMPT`，12 秒看门狗覆盖初始化，主循环和广播配置稳定 5 秒后追加 `CONFIRMED` 并关闭看门狗。
- 连续 3 次未确认会追加 `ROLLBACK_INTENT`，恢复旧槽首扇区，最后才写回旧槽 `KNLT`、清失败候选并记录 `ROLLBACK_DONE`。旧固件若完全没有执行早期恢复代码，仍明确要求 SWire。
- 主机故障注入覆盖两个安装方向各 4,236 个写入截断点、两个回滚方向各 4,236 个写入截断点，共 16,944 个；另覆盖备份擦除 5 个位置、回滚擦除两个方向各 7 个位置。所有结果至少保留一个 ROM 可启动槽，并落入文档化动作。
- 原始 512 KiB 真机备份中 `0x3f000–0x40fff` 共 8 KiB 全为 `0xFF`；`0x76000` 附近存在 BLE 地址数据，M2 不触碰。
- Linux x86_64 TC32 构建通过：M2 镜像约 61–62 KiB，远小于 124 KiB 单槽上限；正常 v9、v14 与断点/故障注入 v8、v10、v12、v13、v15、v16 均通过清单、板型、Telink CRC 校验。当前设备为 B/v16，A/v15 退役。
- 故障注入构建必须显式使用 `HINK_TRIAL_FAIL_TEST=1`、`HINK_OTA_SW_RESET_POINT` 或 `HINK_OTA_PHYSICAL_CUT_POINT`，产物名包含诊断用途标识，普通发布构建不含这些行为。所有真机故障证据保存在仓库外 `output/HINK_OTA_V2_M2_20260913/`。
- 下一停止点：在下一次物理断电前暂停，说明当前槽位、断电窗口和预期 ROM/日志动作，等待人工确认；未获确认不得切断目标板电源。
